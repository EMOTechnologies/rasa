Upgraded Sanic to 25.12 (with `sanic-cors` 2.2, `sanic-routing` 23.12, `sanic-testing` 24.6) and,
as required by `rasa-sdk` 3.20, TensorFlow to 2.21 (running against legacy Keras 2 via `tf-keras`),
protobuf 7, numpy 2, spaCy 3.8, ruamel.yaml 0.19 and other dependencies. The minimum supported Python
version is now 3.10. The Rasa server now builds its Sanic app through an app factory so that multiple
Sanic workers can be started.
